# --- Web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
python-multipart==0.0.10

# --- DB / ORM ---
sqlalchemy==2.0.34
alembic==1.13.2
psycopg2-binary==2.9.9
geoalchemy2==0.15.2

# --- Validation / Settings ---
pydantic==2.9.1
pydantic-settings==2.5.2

# --- Queue ---
celery==5.4.0
redis==5.0.8

# --- Geospatial ---
shapely==2.0.6
pyproj==3.6.1
# 업로드 TIFF bbox 추출 (이정표 5 보강).
rasterio==1.3.10
# DXF 3D 내보내기 (vertex 단위 DEM Z, dem_z_export.py)
ezdxf==1.3.5
# 3D DXF export — sheet index sindex + GPKG I/O
geopandas==0.14.4
# 2D SHP export — UTF-8 DBF와 EPSG:5186 sidecar 직접 생성.
fiona==1.10.1
# GDAL Python 바인딩 — dem_z_export.gdal.BuildVRT 사용.
# 버전은 Dockerfile 의 libgdal-dev 와 매칭해야 하므로 본 파일에 고정하지 않고
# Dockerfile RUN 에서 `pip install GDAL==$(gdal-config --version)` 으로 설치.

# --- Test ---
# 통합 테스트 — docker compose exec backend pytest backend/tests
pytest==7.4.4
httpx==0.27.0
