# Changelog

이 프로젝트의 주요 변경 사항을 기록한다. 버전은 [Semantic Versioning](https://semver.org/)을 따른다.

## [1.2.3] - 2026-07-27

### Added

- 내보내기 팝업 지도에서 원형·다각형·자유형·사각형 관심지역을 지정하고 교차하는 변화탐지 객체만 SHP·DXF·3D DXF·PDF로 내보낼 수 있다.
- 내보내기 지도에서 과년도·당해년도 정사영상, 각 영상 영역, 변화탐지 처리영역을 확인할 수 있다.
- 선택 모드에서 `Shift`를 누른 채 드래그해 여러 폴리곤을 추가 선택할 수 있다.

### Changed

- 상세보기 좌표계를 `TM 중부 (EPSG:5186)` 형식의 짧은 이름으로 표시한다.
- 처리 히스토리를 작업·객체·변화유형·객체 ID·처리의견·처리시각 한 줄 형식으로 정리하고 `생성`·`삭제`·`수정`·`의견`으로 구분한다.
- 폴리곤 꼭짓점 이동·추가·삭제가 끝나면 형태 변경을 즉시 저장하고 선택 모드로 복귀한다.

### Compatibility

- PostgreSQL schema, Compose volume, PM 모델에는 변경이 없다.
- 이전 버전에서 객체 종류 또는 변화유형 변경이 `edit_meta`로 저장된 과거 이력은 `의견`으로 표시될 수 있다. 객체 데이터 자체에는 영향이 없다.

## [1.2.2] - 2026-07-16

### Fixed

- 도로 변화 객체의 수동 편집 목록에서 엔진이 지원하지 않는 `갱신` 옵션을 제거했다.

## [1.2.1] - 2026-07-14

### Added

- 완전 오프라인 서버용 애플리케이션·Docker 이미지 패키지 생성 스크립트를 추가했다.
- 별도 모델 패키지, manifest, SHA-256 생성과 오프라인 이미지 적재·설치·검증 절차를 추가했다.

### Changed

- 오프라인 설치는 이미지를 다시 빌드하지 않고 `docker load`와 `docker compose up --no-build`를 사용한다.
- 최초 설치는 DB·Redis 기동과 마이그레이션을 애플리케이션 시작보다 먼저 수행한다.

## [1.2.0] - 2026-07-14

첫 번째 정식 GitHub Release 기준 버전이다.

### Added

- 대시보드 지도에 프로젝트별 실제 처리영역 오버레이를 추가했다.
- 작업 API에 영상 중첩 범위를 나타내는 `processing_geometry`를 추가했다.
- 다중 과년도·당해년도 영상 처리와 서버 기반 SHP 내보내기를 지원한다.

### Changed

- 프로젝트 선택 시 실제 처리영역을 기준으로 지도가 이동하도록 개선했다.
- SHP 내보내기를 UTF-8 속성과 EPSG:5186 좌표계가 포함된 서버 생성 방식으로 전환했다.
- 장시간 변화탐지 작업의 Redis visibility timeout과 engine worker prefetch 설정을 보강했다.

### Release

- 운영 Docker 이미지와 `/health` 응답에 제품 버전을 노출한다.
